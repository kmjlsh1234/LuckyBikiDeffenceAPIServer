name: Spring Boot & Gradle CI/CD

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    # 1. Checkout code
    - uses: actions/checkout@v3

    # 2. Set up JDK 17
    - name: Set up JDK 21
      uses: actions/setup-java@v3
      with:
        java-version: '21'
        distribution: 'temurin'

    # 3. Grant execute permission for gradlew
    - name: Grant execute permission for gradlew
      run: chmod +x ./LuckyBikiDeffenceAPIServer/gradlew  # 정확한 경로 확인

    # 4. Build with Gradle
    - name: Build with Gradle
      run: ./LuckyBikiDeffenceAPIServer/gradlew clean build --no-daemon  # 정확한 경로 확인
